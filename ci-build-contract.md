# CI build contract

A successful GetJobReady commit must pass dependency installation, product regression contracts, the production frontend build, production artifact validation, server syntax validation, and a local health smoke test. Live-site verification is performed separately after deployment.
