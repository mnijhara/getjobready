import React,{useEffect,useMemo,useRef,useState}from'react';
import{createRoot}from'react-dom/client';
import{Upload,FileText,Target,Mic,ShieldCheck,Sparkles,ArrowRight,CheckCircle2,BriefcaseBusiness,ChevronRight,MessageSquareText,X,Headphones,Volume2,RefreshCw,Check}from'lucide-react';
import'./styles.css';import'./voice.css';import'./mode-tabs.css';
const modules=[{id:'resume',icon:FileText,title:'CV Preparation',text:'Review your CV, improve it, then practise with the final version.',tag:'Start here'},{id:'interview',icon:Mic,title:'AI Audio Interview',text:'A real voice conversation with automatic capture and a visible transcript.',tag:'Practice'},{id:'readiness',icon:ShieldCheck,title:'Corporate Ready',text:'Build communication, confidence, feedback and workplace habits.',tag:'Thrive'},{id:'ai',icon:Sparkles,title:'AI at Work',text:'Learn practical AI workflows that make you faster and sharper.',tag:'Future-ready'},{id:'demo',icon:Target,title:'Impress the Interviewer',text:'Turn a company problem into a polished product concept and demo.',tag:'Stand out'}];
const fallback=mode=>({score:72,headline:mode==='general'?'Your CV has a solid base.':'Your profile has a solid base — now make it role-specific.',summary:'Strengthen evidence, clarity and outcomes before you interview.',highlights:['Clear academic foundation','Transferable problem-solving skills','Strong learning intent'],gaps:['Add measurable outcomes','Make ownership explicit','Connect your strongest evidence to the target role'],cvImprovements:['Lead bullets with action + outcome','Quantify scope only where your CV supports it','Move the strongest evidence higher'],rewrittenBullets:['Led a project using a structured approach to improve a measurable outcome.','Collaborated with a cross-functional team to deliver a project within the agreed timeline.'],plan:['Create a 90-second introduction','Strengthen your top three CV bullets','Build three STAR stories','Research the company and role','Practise five interview questions','Complete a realistic voice interview','Review feedback and repeat'],interviewQuestions:['Tell me about yourself and the experience you are most proud of.','Walk me through a project where you solved a difficult problem.','Tell me about a time you took ownership.','What is one piece of feedback that changed how you work?','What would you do in your first 30 days?']});
const readSession=(key,f='')=>{try{return sessionStorage.getItem(key)||f}catch{return f}};
const saveSession=(key,v)=>{try{sessionStorage.setItem(key,v);if(key==='gjr_career')sessionStorage.setItem('gjr_career',v);if(key==='gjr_cv_text')sessionStorage.setItem('gjr_cv_text',v);if(key==='gjr_jd_text')sessionStorage.setItem('gjr_jd_text',v)}catch{}};
async function post(path,body){const r=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`Request failed (${r.status})`);return data}
async function pdfText(file){const lib=await import('pdfjs-dist');const pdfjs=lib.default&&lib.default.getDocument?lib.default:lib;pdfjs.GlobalWorkerOptions.workerSrc='/pdf.worker.mjs';const pdf=await pdfjs.getDocument({data:await file.arrayBuffer(),disableWorker:true}).promise;let out='';for(let i=1;i<=pdf.numPages;i++){const p=await pdf.getPage(i);const t=await p.getTextContent();out+=t.items.map(x=>x.str).join(' ')+'\n'}return out.trim()}
async function docxText(file){const mammoth=await import('mammoth');const result=await mammoth.extractRawText({arrayBuffer:await file.arrayBuffer()});return String(result.value||'').trim()}
async function readFile(file){if(file.type==='text/plain')return file.text();if(/\.pdf$/i.test(file.name))return pdfText(file);if(/\.docx$/i.test(file.name))return docxText(file);throw new Error('Unsupported file type')}
function localReview(cv,jd,mode){const base=fallback(mode);const words=cv.trim().split(/\s+/).filter(Boolean).length;return {...base,score:Math.max(62,Math.min(88,base.score+(words>450?7:words>220?3:0))),summary:mode==='specific'?`Local CV review is ready. Add evidence that directly connects your experience to this job description (${Math.min(3,Math.max(1,Math.round(jd.length/1200)))} priority areas identified).`:`Local CV review is ready. Your draft is editable below and can be improved before the interview.`}}
function localImprove(cv){const lines=cv.split(/\n+/).map(x=>x.trim()).filter(Boolean);if(!lines.length)return cv;return lines.map((line,i)=>{if(i<3)return line;const clean=line.replace(/^[•●▪-]\s*/,'');if(clean.length<45||/[.!?]$/.test(clean))return line;return `• ${clean.replace(/^I\s+/i,'').replace(/\s+/g,' ')}.`}).join('\n')}
function App(){const[screen,setScreen]=useState('home'),[career,setCareer]=useState(()=>readSession('gjr_career','job')),[prep,setPrep]=useState(()=>readSession('gjr_cv_mode','specific'));const[cv,setCv]=useState(()=>readSession('gjr_cv_text','')),[jd,setJd]=useState(()=>readSession('gjr_jd_text','')),[cvFile,setCvFile]=useState(null),[jdFile,setJdFile]=useState(null);const[loading,setLoading]=useState(false),[result,setResult]=useState(null),[qIndex,setQIndex]=useState(0),[answers,setAnswers]=useState([]);const questions=useMemo(()=>result?.interviewQuestions?.length?result.interviewQuestions:fallback(prep).interviewQuestions,[result,prep]);const choosePrep=m=>{setPrep(m);saveSession('gjr_cv_mode',m);setScreen('resume')};const changeCareer=v=>{setCareer(v);sessionStorage.setItem('gjr_career',v)};const parseFile=async(kind,file)=>{if(!file)return;const ok=kind==='cv'?/\.(pdf|txt|docx)$/i.test(file.name):/\.(pdf|txt)$/i.test(file.name);if(!ok){alert(kind==='cv'?'Please upload a PDF, DOCX or TXT CV.':'Please upload a PDF or TXT job description.');return}try{const text=await readFile(file);if(!text.trim())throw new Error('The file contains no readable text. Please paste the text instead.');if(kind==='cv'){setCvFile(file);setCv(text);sessionStorage.setItem('gjr_cv_text',text)}else{setJdFile(file);setJd(text);sessionStorage.setItem('gjr_jd_text',text)}}catch(e){console.error(e);alert(`We could not read that file. ${e.message||'Please paste the text instead.'}`)}};const clearFile=kind=>{if(kind==='cv'){setCvFile(null);setCv('');try{sessionStorage.removeItem('gjr_cv_text')}catch{}}else{setJdFile(null);setJd('');try{sessionStorage.removeItem('gjr_jd_text')}catch{}};const analyze=async()=>{if(!cv.trim())return alert('Upload your CV or paste your CV text.');if(prep==='specific'&&!jd.trim())return alert('Upload or paste the job description for a role-specific preparation.');setLoading(true);try{let data;try{data=await post('/api/analyze',{cv,jd:prep==='general'?'':jd,career,mode:prep})}catch(e){console.warn('AI review unavailable; using local review',e);data=localReview(cv,jd,prep)}setResult(data);sessionStorage.setItem('gjr_cv_mode',prep);sessionStorage.setItem('gjr_cv_text',cv);sessionStorage.setItem('gjr_jd_text',prep==='general'?'':jd);setScreen('cvstudio')}finally{setLoading(false)}};const saveFinal=text=>{setCv(text);sessionStorage.setItem('gjr_cv_ready','1');sessionStorage.setItem('gjr_cv_improved',text);sessionStorage.setItem('gjr_cv_text',text)};const startInterview=text=>{if(text)setCv(text);setQIndex(0);setAnswers([]);setScreen('interview')};if(screen==='home')return <Home career={career}setCareer={changeCareer}choosePrep={choosePrep}openModule={setScreen}/>;if(screen==='resume')return <Workspace title={prep==='general'?'General CV Preparation':'CV + Job Description'} subtitle={prep==='general'?'Review your CV without a target role. Save the final version before interviewing.':'Upload your CV and one specific JD. Improve the CV before you practise the role-specific interview.'} icon={<FileText/>} back={()=>setScreen('home')}><Prep prep={prep}setPrep={setPrep}cv={cv}setCv={setCv}jd={jd}setJd={setJd}cvFile={cvFile}jdFile={jdFile}parseFile={parseFile}clearFile={clearFile}analyze={analyze}loading={loading}/></Workspace>;if(screen==='cvstudio')return <Workspace title="Improve your CV first" subtitle={prep==='general'?'Review, edit and save your CV. Your interview will use the final version.':'Make your CV stronger for this role before you practise the interview.'} icon={<Sparkles/>} back={()=>setScreen('resume')}><CVStudio result={result||fallback(prep)}initial={cv}mode={prep}onSave={saveFinal}onContinue={startInterview}/></Workspace>;if(screen==='interview')return <Workspace title="AI Audio Interview" subtitle={prep==='general'?'General interview grounded in your final CV.':'Role-specific interview grounded in your final CV + JD.'} icon={<Mic/>} back={()=>setScreen('cvstudio')}><VoiceInterview cv={cv}jd={jd}mode={prep}career={career}question={questions[qIndex]}turn={qIndex+1}maxTurns={7}history={answers}onTurn={(d,a)=>{setAnswers(x=>[...x,{question:questions[qIndex],answer:a,evaluation:d?.evaluation}]);if(!d.done&&d.nextQuestion){setResult(r=>({...r,interviewQuestions:[...(r?.interviewQuestions||questions),d.nextQuestion]}));setQIndex(i=>i+1)}}}onDone={d=>{setResult(r=>({...r,feedback:d}));setScreen('feedback')}}/></Workspace>;if(screen==='feedback')return <Workspace title="Interview Feedback" subtitle="Your transcript, strengths and next actions." icon={<MessageSquareText/>} back={()=>setScreen('home')}><Feedback data={result?.feedback}answers={answers}/></Workspace>;return <Workspace title={modules.find(x=>x.id===screen)?.title||'GetJobReady'} subtitle={modules.find(x=>x.id===screen)?.text||''} icon={<Sparkles/>} back={()=>setScreen('home')}><Module id={screen}career={career}/></Workspace>}
function Header({onHow,back}){return <header><div className="header-left">{back&&<button className="back"onClick={back}>← Back</button>}</div><div className="brand"><span className="logo">GJ</span><span>GetJobReady<span className="dot">.online</span></span></div>{onHow?<button className="ghost"onClick={onHow}><Sparkles size={15}/> How it works</button>:<div className="header-spacer"/>}</header>}
function Home({career,setCareer,choosePrep,openModule}){const[how,setHow]=useState(false);return <div className="app"><Header onHow={()=>setHow(true)}/><main className="hero"><div className="eyebrow"><Sparkles size={15}/> {career==='internship'?'INTERNSHIP TRACK':'FULL-TIME TRACK'} · AI-POWERED CAREER READINESS</div><h1>Your degree got you here.<br/><em>Let’s get you job-ready.</em></h1><p className="lead">{career==='internship'?'Build a strong campus-to-internship story, sharpen your CV and practise realistic recruiter questions.':'Turn your CV into a stronger professional story, prepare for the exact role and practise realistic corporate interviews.'}</p><div className="career-toggle"><button className={career==='internship'?'active':''}onClick={()=>setCareer('internship')}>☀️ Summer Internship</button><button className={career==='job'?'active':''}onClick={()=>setCareer('job')}>💼 Full-time Job</button></div><button className="primary hero-btn"onClick={()=>choosePrep('general')}>Start with my CV <ArrowRight size={18}/></button><div className="proof"><span><CheckCircle2 size={16}/> Improve before interview</span><span><CheckCircle2 size={16}/> Automatic voice capture</span><span><CheckCircle2 size={16}/> Transcript + feedback</span></div></main><section className="module-grid">{modules.map(m=><button className="module-card"key={m.id}onClick={()=>m.id==='resume'?choosePrep('general'):m.id==='interview'?choosePrep('general'):openModule(m.id)}><div className="module-top"><span className="module-icon"><m.icon size={21}/></span><span className="pill">{m.tag}</span></div><h3>{m.title}</h3><p>{m.text}</p><span className="card-link">{m.id==='interview'?'Start with your CV':'Open'} <ChevronRight size={16}/></span></button>)}</section><section className="roadmap"><div><span className="eyebrow">THE JOURNEY</span><h2>Prepare → practise → improve.</h2></div><div className="steps"><div><b>01</b><span>Prepare</span><small>CV review + editor</small></div><div><b>02</b><span>Practise</span><small>Hands-free voice</small></div><div><b>03</b><span>Improve</span><small>Transcript + feedback</small></div></div></section><footer>Built for students entering their first internship or corporate role.</footer>{how&&<div className="modal"onClick={e=>e.target===e.currentTarget&&setHow(false)}><div className="modal-card"><button className="modal-x"onClick={()=>setHow(false)}><X size={18}/></button><span className="eyebrow">HOW GETJOBREADY WORKS</span><h2>Prepare → practise → improve.</h2><div className="how-steps"><div><b>01</b><strong>Upload your CV</strong><span>Use General CV mode or add one specific JD.</span></div><div><b>02</b><strong>Improve before interviewing</strong><span>AI reviews your CV and gives you an editable version. If AI is temporarily unavailable, the CV studio still opens with a useful local review.</span></div><div><b>03</b><strong>Have a real voice interview</strong><span>AI asks the question aloud, then your answer is captured into the live transcript automatically.</span></div><div><b>04</b><strong>Get adaptive feedback</strong><span>Review your transcript, strengths and next actions.</span></div></div><button className="primary wide"onClick={()=>{setHow(false);choosePrep('general')}}>Start with my CV <ArrowRight size={18}/></button></div></div>}</div>}
function Workspace({title,subtitle,icon,back,children}){return <div className="app"><Header back={back}/><main className="workspace"><div className="workspace-head"><span className="big-icon">{icon}</span><div><div className="eyebrow">YOUR PREPARATION</div><h1>{title}</h1><p>{subtitle}</p></div></div>{children}</main></div>}
function Prep({prep,setPrep,cv,setCv,jd,setJd,cvFile,jdFile,parseFile,clearFile,analyze,loading}){return <><div className="mode-switch"role="tablist"><button className={prep==='general'?'selected':''}onClick={()=>setPrep('general')}><span>📄</span><b>General CV</b>{prep==='general'&&<Check size={16}/>}</button><button className={prep==='specific'?'selected':''}onClick={()=>setPrep('specific')}><span>🎯</span><b>CV + specific JD</b>{prep==='specific'&&<Check size={16}/>}</button></div><div className="form-grid"><div className="input-card"><div className="label"><FileText size={17}/> Your CV</div><label className="dropzone"><Upload size={27}/><b>{cvFile?.name||'Upload your CV'}</b><span>{cvFile?'CV loaded · ready for review':'PDF, DOCX or TXT · or paste below'}</span><input type="file"accept=".pdf,.txt,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document,text/plain"onChange={e=>parseFile('cv',e.target.files?.[0])}/></label>{cvFile&&<button className="clear-file"onClick={()=>clearFile('cv')}><X size={15}/> Remove file</button>}<div className="or"><span>or paste CV text</span></div><textarea value={cv}onChange={e=>{setCv(e.target.value);sessionStorage.setItem('gjr_cv_text',e.target.value)}}placeholder="Paste your CV here…"/></div>{prep==='specific'?<div className="input-card"><div className="label"><BriefcaseBusiness size={17}/> Target Job Description</div><label className="dropzone"><Upload size={27}/><b>{jdFile?.name||'Upload the job description'}</b><span>{jdFile?'JD loaded · role matching ready':'PDF or TXT · or paste below'}</span><input type="file"accept=".pdf,.txt,application/pdf,text/plain"onChange={e=>parseFile('jd',e.target.files?.[0])}/></label>{jdFile&&<button className="clear-file"onClick={()=>clearFile('jd')}><X size={15}/> Remove JD</button>}<div className="or"><span>or paste JD text</span></div><textarea className="tall"value={jd}onChange={e=>{setJd(e.target.value);sessionStorage.setItem('gjr_jd_text',e.target.value)}}placeholder="Paste the exact job description here…"/></div>:<div className="input-card mode-explainer"><div className="label"><Sparkles size={17}/> General CV mode</div><div className="module-hero"><span className="eyebrow">CV ONLY</span><h2>No JD needed.</h2><p>Your CV is analysed on its own. You will edit and save the version you want the AI interviewer to use.</p></div></div>}<div className="full action-row"><div><b>{prep==='general'?'Ready to improve your CV?':'Ready to improve and match your CV?'}</b><span>Nothing sends you straight to interview. CV review always comes first.</span></div><button className="primary"disabled={loading||!cv.trim()||prep==='specific'&&!jd.trim()}onClick={analyze}>{loading?'Reviewing your CV…':<>Review & improve my CV <ArrowRight size={18}/></>}</button></div></div></>}
function CVStudio({result,initial,mode,onSave,onContinue}){const[draft,setDraft]=useState(()=>String(initial||''));const[improving,setImproving]=useState(false),[improved,setImproved]=useState(false),[error,setError]=useState('');const run=async()=>{if(!draft.trim())return;setImproving(true);setError('');try{const r=await post('/api/improve-cv',{cv:draft,review:result,mode});if(r.cv){setDraft(r.cv);setImproved(true)}else throw new Error('No improved draft returned')}catch(e){console.warn(e);setDraft(localImprove(draft));setImproved(true);setError('AI improvement is temporarily unavailable, so a local editing pass was applied. You can keep editing it.')}finally{setImproving(false)}};return <div className="studio"><div className="score-card"><div><span className="eyebrow">AI CV REVIEW</span><h2>{result.headline}</h2><p>{result.summary}</p></div><div className="score-ring"><strong>{result.score}</strong><small>/100</small></div></div><div className="insights"><div><h3>What to improve</h3>{result.gaps?.map(x=><p key={x}>• {x}</p>)}</div><div><h3>Keep these strengths</h3>{result.highlights?.map(x=><p key={x}>✓ {x}</p>)}</div></div><div className="editor-card"><div className="editor-head"><div className="label"><FileText size={17}/> Your editable CV</div><span>Autosaved locally</span></div><textarea value={draft}onChange={e=>{setDraft(e.target.value);setImproved(false);sessionStorage.setItem('gjr_cv_text',e.target.value)}}placeholder="Your CV text will appear here. Edit anything you want before your interview."/><div className="editor-actions"><button className="secondary"onClick={run}disabled={improving||!draft.trim()}>{improving?'Improving…':<>✨ Analyse & improve my CV</>}</button>{improved&&<span className="saved-note"><CheckCircle2 size={15}/> Improved draft ready</span>}</div>{error&&<p className="inline-note">{error}</p>}</div><div className="continue-card"><div><b>Next: live interview</b><span>Save your final CV first. The interviewer will use exactly this version.</span></div><button className="primary"disabled={!draft.trim()}onClick={()=>{onSave(draft);onContinue(draft)}}>Save & continue to live interview <ArrowRight size={18}/></button></div></div>}
function VoiceInterview({cv,jd,mode,career,question,turn,maxTurns,history,onTurn,onDone}){const[status,setStatus]=useState('starting'),[transcript,setTranscript]=useState(''),[turns,setTurns]=useState(history||[]),[permission,setPermission]=useState(false),rec=useRef(null),started=useRef(false),submitting=useRef(false);const supported=typeof window!=='undefined'&&('webkitSpeechRecognition'in window||'SpeechRecognition'in window);useEffect(()=>{setTurns(history||[])},[history]);const speakAndListen=()=>{if(started.current)return;started.current=true;window.speechSynthesis?.cancel();const u=new SpeechSynthesisUtterance(question);u.lang='en-IN';u.rate=.96;u.pitch=1;u.onend=()=>beginRecognition();window.speechSynthesis?.speak(u)};const beginRecognition=()=>{if(!supported){setStatus('unsupported');return}const SR=window.SpeechRecognition||window.webkitSpeechRecognition;const r=new SR();r.lang='en-IN';r.interimResults=true;r.continuous=false;r.maxAlternatives=1;r.onstart=()=>{setStatus('listening');setPermission(true)};r.onresult=e=>{let text='';for(let i=e.resultIndex;i<e.results.length;i++)text+=e.results[i][0].transcript+' ';setTranscript(text.trim())};r.onend=()=>{const answer=transcript.trim();setStatus(answer?'thinking':'idle');if(answer&&!submitting.current){submitting.current=true;submit(answer).finally(()=>{submitting.current=false})}};r.onerror=e=>{if(e.error==='not-allowed'||e.error==='service-not-allowed')setStatus('permission');else if(e.error!=='aborted')setStatus('error')};rec.current=r;try{r.start()}catch{setStatus('error')}};const startJourney=()=>{started.current=false;speakAndListen()};const submit=async(answer)=>{try{let data;try{data=await post('/api/interview-turn',{cv,jd,mode,career,question,answer,history:turns,turn,maxTurns})}catch(e){console.warn(e);const next=fallback(mode).interviewQuestions[turn]||'Tell me what you would improve in your next answer.';data={done:turn>=maxTurns,nextQuestion:next,evaluation:{score:70,notes:'Clear answer captured. Add a specific example and measurable outcome.'},finalFeedback:{score:70,strengths:['You answered with your own experience'],improvements:['Add a concrete example','Quantify the result'],nextAction:'Repeat the interview with one stronger STAR story.'}}}setTurns(x=>[...x,{question,answer,evaluation:data.evaluation}]);setTranscript('');if(data.done)onDone(data.finalFeedback||data);else{onTurn(data,answer);started.current=false}}catch(e){setStatus('error');alert(e.message||'We could not submit this answer. Please try again.')}};useEffect(()=>{setStatus('idle');started.current=false;setTranscript('');window.speechSynthesis?.cancel();return()=>{rec.current?.abort();window.speechSynthesis?.cancel()}},[question,turn]);return <div className="interview"><div className="question-card"aria-live="polite"><span className="eyebrow">QUESTION {turn} OF {maxTurns}</span><h2>{question}</h2><p>{status==='thinking'?'Evaluating your answer…':status==='listening'?'I’m listening. Finish naturally — I’ll capture it automatically.':'The question will be spoken aloud. Tap the mic once to begin; after that the interview runs hands-free.'}</p></div><div className={`voice-card handsfree ${status}`}onClick={status==='idle'||status==='permission'||status==='error'?startJourney:undefined}role="button"tabIndex={0}onKeyDown={e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();startJourney()}}}><div className={`mic ${status}`}><Mic size={38}/>{status==='listening'&&<i/>}</div><div className="voice-state"><b>{status==='listening'?'Listening…':status==='thinking'?'AI is evaluating…':status==='permission'?'Tap to allow microphone':status==='unsupported'?'Voice input unavailable':'Tap mic to begin'}</b><span>{supported?'No submit button. Your answer is captured when you finish speaking.':'Use Chrome on Android or desktop for voice input.'}</span></div></div><div className="transcript-card"><div className="transcript-head"><div className="label"><Headphones size={17}/> Live transcript</div><span>{transcript?'Capturing':'Waiting for your answer'}</span></div><p className={transcript?'live':''}>{transcript||'Your spoken answer will appear here in real time.'}</p></div>{permission&&<div className="interview-hint"><Volume2 size={15}/> Question audio · automatic answer capture · no manual submit</div>}</div>}
function Feedback({data,answers}){const d=data||{score:70,strengths:['You completed the interview'],improvements:['Add evidence and measurable outcomes'],nextAction:'Repeat the interview with sharper examples.'};return <div className="feedback"><div className="score-card"><div><span className="eyebrow">YOUR INTERVIEW</span><h2>Good start. Now make your evidence sharper.</h2><p>Your answers are saved in this session and shown below.</p></div><div className="score-ring"><strong>{d.score||70}</strong><small>/100</small></div></div><div className="insights"><div><h3>Strengths</h3>{d.strengths?.map(x=><p key={x}>✓ {x}</p>)}</div><div><h3>Next improvements</h3>{d.improvements?.map(x=><p key={x}>• {x}</p>)}</div></div><div className="transcript-review"><div className="label"><MessageSquareText size={17}/> Interview transcript</div>{(answers||[]).map((x,i)=><div className="turn-review"key={i}><b>Q{i+1}. {x.question}</b><p>{x.answer}</p></div>)}</div><div className="continue-card"><div><b>Next action</b><span>{d.nextAction}</span></div><button className="primary"onClick={()=>location.reload()}><RefreshCw size={17}/> Practise again</button></div></div>}
function Module({id,career}){const[loading,setLoading]=useState(false),[data,setData]=useState(null),[error,setError]=useState('');const[company,setCompany]=useState(''),[problem,setProblem]=useState(''),[idea,setIdea]=useState('');const cv=readSession('gjr_cv_text','');const runCoach=async()=>{setLoading(true);setError('');try{let d;try{d=await post('/api/coach',{module:id==='ai'?'ai':'corporate',context:cv,career})}catch(e){d={diagnosis:'Start with one practical habit this week.',score:70,weeklyHabit:'Write one concise outcome-led update every day.',sevenDayPlan:['Audit one communication habit','Practise a concise update','Ask for one feedback point','Rewrite one weak CV bullet with evidence','Use AI to structure one task','Reflect on what improved','Repeat the strongest habit']}}setData(d)}finally{setLoading(false)}};const runDemo=async()=>{if(!problem.trim()){setError('Describe the company problem first.');return}setLoading(true);setError('');try{setData(await post('/api/demo',{company,problem,idea}))}catch(e){setData({title:'Focused solution concept',tagline:'A simple workflow that reduces friction and creates measurable value.',impact:'Explain the user, problem, workflow and one measurable outcome in your interview.'})}finally{setLoading(false)}};if(id==='readiness'||id==='ai')return <div className="module-panel"><span className="eyebrow">{career==='internship'?'INTERNSHIP':'FULL-TIME'} TRACK</span><h2>{id==='readiness'?'Corporate Ready':'AI at Work'}</h2><p>{id==='readiness'?'Build practical habits for communication, feedback, priorities and resilience.':'Learn practical AI workflows for research, writing, analysis, meetings and responsible automation.'}</p><button className="primary"disabled={loading}onClick={runCoach}>{loading?'Building your plan…':<>Build my 7-day plan <ArrowRight size={18}/></>}</button>{error&&<p role="alert">{error}</p>}{data&&<div className="module-result"><h3>{data.diagnosis||'Your personalised plan'}</h3>{data.score&&<p><b>Readiness score:</b> {data.score}/100</p>}{data.weeklyHabit&&<p><b>Weekly habit:</b> {data.weeklyHabit}</p>}{data.sevenDayPlan&&<><h4>7-day plan</h4>{data.sevenDayPlan.map((x,i)=><p key={i}><b>Day {i+1}:</b> {x}</p>)}</>}</div>}</div>;return <div className="module-panel"><span className="eyebrow">STAND OUT IN THE INTERVIEW</span><h2>Build a credible product concept</h2><p>Turn a real company problem into a focused solution you can explain to an interviewer.</p><div className="input-card"><div className="label"><BriefcaseBusiness size={17}/> Company</div><input value={company}onChange={e=>setCompany(e.target.value)}placeholder="e.g. a target employer"/><div className="label"><Target size={17}/> Business problem</div><textarea value={problem}onChange={e=>setProblem(e.target.value)}placeholder="What problem should the company solve?"/><div className="label"><Sparkles size={17}/> Your idea <span>(optional)</span></div><textarea value={idea}onChange={e=>setIdea(e.target.value)}placeholder="Your initial solution idea…"/><button className="primary"disabled={loading||!problem.trim()}onClick={runDemo}>{loading?'Building concept…':<>Build my interview demo <ArrowRight size={18}/></>}</button></div>{error&&<p role="alert">{error}</p>}{data&&<div className="module-result"><span className="eyebrow">PROTOTYPE CONCEPT</span><h3>{data.title}</h3><p><b>{data.tagline}</b></p><p>{data.impact}</p></div>}</div>}
createRoot(document.getElementById('root')).render(<App/>);
