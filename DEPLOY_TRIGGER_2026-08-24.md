# Production deployment trigger

This file intentionally triggers the main-branch production build after the GetJobReady UI and deployment corrections.

The production UI entrypoint is `index.html` → `app.js`, which contains the current Instagram-style GetJobReady experience. This marker can be removed after deployment if desired.
